# change the project name in the deploy_log.sh
./deploy_log.sh

# it will:
#   enable cluster log to write to log analytics workspace
#   config the agent
#   deploy ClusterRoleBinding, ServiceAccount, log writer, log follow
